# M16-P0 预览发布验收报告

> 目标版本：`v0.1.0-preview.1`
> 
> 状态：候选构建、自动化和 release EXE 冒烟已通过；仍需用户完成真实安装矩阵才能创建 tag。

## 1. 发布边界

本次是开发、自用和受控测试用途的 Windows 预览快照，不是正式 GA 发行：

- 应用内部版本保持 `0.1.0`，Git 预览标签使用 `v0.1.0-preview.1`。
- 只交付 x64 NSIS 安装包，不建立自动更新服务，不作稳定兼容承诺。
- 安装包暂不签名，可能触发 Windows SmartScreen 未知发布者提示。
- 角色名称、角色素材和应用图标仍待公开发行权利审查；未通过前不公开推广或商业分发。
- M16 在本次 tag 后仍保持进行中，正式 `v0.1.0` 需要完成升级/回滚、低端性能、许可和公开发行检查。

## 2. 已完成的发布准备

- [x] 根 README 从 Tauri 模板重写为安装、交互、隐私、备份、开发和许可说明。
- [x] 新增 `CHANGELOG.md`，区分预览标签与正式稳定版本。
- [x] 安装包产品名改为“小洛宝”，Cargo 描述和作者占位符已清理。
- [x] 保留历史应用标识符 `com.tauri-app.ltypet`，避免现有设置、记忆和密钥无迁移地换目录。
- [x] 主 WebView 启用生产 CSP；动作调试控制台继续受 `import.meta.env.DEV` 构建门保护。
- [x] 移除未使用的 opener 前后端依赖和 capability，发布目标收窄为 NSIS。
- [x] 移除未使用的 Tauri 模板静态资源，应用图标改由现有角色源稿生成。
- [x] 增加 `npm run release:check` 和 `npm run release:verify`，检查版本同步、CSP、调试入口、依赖和运行时秘密文件。

## 3. 自动化结果

实施完成后填写真实结果，不提前把未运行检查标为通过。

| 检查                                                          | 结果                                               |
| ----------------------------------------------------------- | ------------------------------------------------ |
| `node --version`                                            | `v22.20.0`                                       |
| `npm ls --depth=0`                                          | 通过；移除 opener 后无 extraneous/invalid 依赖            |
| `npm run release:verify`                                    | 通过；45 个文件、478 项 Vitest，生产构建 138 modules，发布静态检查通过 |
| `cargo test --manifest-path src-tauri/Cargo.toml`           | 通过；29 项 Rust 测试                                  |
| `cargo check --manifest-path src-tauri/Cargo.toml`          | 通过                                               |
| `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` | 通过                                               |
| `npm run tauri build`                                       | 通过；生成 x64 NSIS 安装包                               |
| release EXE 启动冒烟                                            | 通过；进程启动 5 秒后仍存活并已清理测试进程                          |
| `git diff --check`                                          | 通过；仅有工作区换行符提示                                    |

候选安装包：

```text
src-tauri/target/release/bundle/nsis/小洛宝_0.1.0_x64-setup.exe
大小：3,229,529 bytes（3.08 MiB）
SHA-256：9956FAAEF180E726809B5946BCB4C28CED7863100C56E4C0241946D95432DB52
签名：NotSigned
```

## 4. 真实 Windows 安装矩阵

请使用最终生成的 `src-tauri/target/release/bundle/nsis/小洛宝_0.1.0_x64-setup.exe` 验证：

### A. 全新安装

- [x] 在未安装小洛宝的 Windows 用户中完成安装。
- [x] 启动后角色可见；若主窗口隐藏，左键托盘图标可以恢复。
- [x] 单击招手、拖动、右键菜单、设置窗口和对话窗口正常。
- [x] `Ctrl+Shift+Q`、角色菜单和托盘菜单均可完全退出进程。
- [x] 生产界面中不存在“动作控制台”等开发调试入口。

### B. 数据与恢复

- [x] 修改置顶、点击穿透、番茄钟等设置，退出并重启后保留。
- [x] 新增长期记忆后重启，内容和羁绊仍存在。
- [x] 配置本机模型或 Mock；确认普通设置文件不出现 API key 明文。
- [x] 退出应用、备份 `%APPDATA%\com.tauri-app.ltypet`、恢复目录后数据可读取。
- [x] 明确记录 DPAPI 密钥不能作为跨机器/跨用户可移植备份。

### C. 覆盖安装与卸载

- [x] 在现有开发数据上安装预览包，设置迁移成功且主窗口可恢复。
- [x] 再次运行相同安装包可以修复/覆盖安装，不产生重复启动项或重复托盘实例。
- [x] 卸载后程序文件和启动入口移除。
- [x] 核对卸载是否保留 `%APPDATA%\com.tauri-app.ltypet`；无论结果如何都在发布说明中如实记录。

### D. 运行回归

- [x] 100% 与 150% DPI 下角色命中、点击穿透和菜单位置正确。
- [x] 多显示器和负坐标环境下拖动、居中和位置恢复正常。
- [x] 系统音乐观察、TTS 和 Agent 全部关闭时，基础桌宠保持离线可用。
- [x] 开启系统音乐反应后只读取播放状态；停止后视觉恢复待机。
- [x] 外部 Provider 失败、插件桥接失败或记忆文件损坏时仍能从托盘退出。

## 5. tag 门槛与回滚

只有自动化检查、NSIS 构建和上述真实安装关键路径通过后，才执行：

```powershell
git tag -a v0.1.0-preview.1 -m "小洛宝 v0.1.0 preview 1"
git push origin v0.1.0-preview.1
```

预览版没有自动更新器。回滚方式是：退出小洛宝、备份应用数据目录、卸载当前包并安装上一预览包；如果设置 schema 已升级，不承诺旧版能够读取新版数据，必要时同步恢复对应版本的数据备份。

## 6. 未关闭风险

- 角色 IP、名称、图标和衍生素材尚未完成公开发行授权审查。
- 依赖许可证与 notice 尚需在公开发行前形成完整、可随包交付的清单。
- 安装包没有代码签名；低端硬件、ARM64、早期 Windows 10 和远程桌面未形成性能/兼容基线。
- 应用标识符沿用早期模板值是有意兼容决策；正式版前若要更名，必须先实现数据目录和 DPAPI 密钥迁移。
